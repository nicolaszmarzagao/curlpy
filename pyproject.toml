[project]
name = "curlpy"
version = "0.1.0"
description = "A simple curl clone in Python"
requires-python = ">=3.10"

[project.scripts]
curlpy = "curlpy.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
